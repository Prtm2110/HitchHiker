import { Router } from "express";

const route = Router();


//user should not now the endpoints
//mask the endpoint 

//service layer

const repositoryInterface  = () =>{
//MVC -> model view controller

    //import jpa repository
    //configure a layer betn springboot app
    // and database
    // -> hibernate
    // dependiencies -> node == maven
    // pom - package.js
    // dependencies -> jar files
    //application.properties == .env -> server details , db details , hibernate->jpa->jpa configuration 
    //devTool -> longBook == nodemon
    // jar vs war -> how to delploy jar and war -> change the way to deploy backend
    // server for springboot apache tomcat
    //thymley -> part og springboot project
    //same server as  spring boot
    //   


    //monolithic
    //in one go 
    //microservices
    //scalability is easy
    //point of contact -> api gateway
    //client will interract with api gateway
    //different service on different server


    //response
    //-->message
    //-->count
    //-->data ---------> vector(as size is dynamic)
    //-->status code




}